# Fake secret fixtures for scanner smoke tests only.
#
# These values are documentation/example keys, not production credentials.
# Path is allowlisted in `.gitleaks.toml` so CI secret scanning does not fail
# on intentional fixtures. Local gitleaks against this directory should still
# report findings when run without the repo config.
