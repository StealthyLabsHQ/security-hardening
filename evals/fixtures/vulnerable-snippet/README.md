# Vulnerable snippet fixtures

These files exist only to smoke-test `scripts/secure-review.py` and the Semgrep
rules under `semgrep/`. They intentionally contain insecure patterns.

Do not treat them as examples to copy. Prefer the defensive patterns in
`references/`.
