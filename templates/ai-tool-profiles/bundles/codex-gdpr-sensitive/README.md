# Codex GDPR-Sensitive Bundle

Recommended start:

```bash
codex --suggest
```

If edits are required, move to:

```bash
codex --auto-edit
```

Do not use `--full-auto` on a GDPR-sensitive repo unless the environment is isolated, the data is synthetic or masked, and the approval model is explicitly accepted by the repo owner.
